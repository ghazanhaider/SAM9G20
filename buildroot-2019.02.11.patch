diff --git a/Makefile b/Makefile
index 0cbe1076c5..c85e9eb306 100644
--- a/Makefile
+++ b/Makefile
@@ -747,12 +747,12 @@ target-finalize: $(PACKAGES) host-finalize
 	./support/scripts/check-uniq-files -t staging $(BUILD_DIR)/packages-file-list-staging.txt
 	./support/scripts/check-uniq-files -t host $(BUILD_DIR)/packages-file-list-host.txt
 	$(foreach hook,$(TARGET_FINALIZE_HOOKS),$($(hook))$(sep))
-	rm -rf $(TARGET_DIR)/usr/include $(TARGET_DIR)/usr/share/aclocal \
-		$(TARGET_DIR)/usr/lib/pkgconfig $(TARGET_DIR)/usr/share/pkgconfig \
-		$(TARGET_DIR)/usr/lib/cmake $(TARGET_DIR)/usr/share/cmake
+#	rm -rf $(TARGET_DIR)/usr/include $(TARGET_DIR)/usr/share/aclocal \
+#		$(TARGET_DIR)/usr/lib/pkgconfig $(TARGET_DIR)/usr/share/pkgconfig \
+#		$(TARGET_DIR)/usr/lib/cmake $(TARGET_DIR)/usr/share/cmake
 	find $(TARGET_DIR)/usr/{lib,share}/ -name '*.cmake' -print0 | xargs -0 rm -f
-	find $(TARGET_DIR)/lib/ $(TARGET_DIR)/usr/lib/ $(TARGET_DIR)/usr/libexec/ \
-		\( -name '*.a' -o -name '*.la' \) -print0 | xargs -0 rm -f
+#	find $(TARGET_DIR)/lib/ $(TARGET_DIR)/usr/lib/ $(TARGET_DIR)/usr/libexec/ \
+#		\( -name '*.a' -o -name '*.la' \) -print0 | xargs -0 rm -f
 ifneq ($(BR2_PACKAGE_GDB),y)
 	rm -rf $(TARGET_DIR)/usr/share/gdb
 endif
index 75ce3b2f6a..c28b9bcd57 100644
--- a/boot/uboot/uboot.mk
+++ b/boot/uboot/uboot.mk
@@ -274,6 +274,7 @@ define UBOOT_GENERATE_ENV_IMAGE
 		>$(@D)/buildroot-env.txt
 	$(HOST_DIR)/bin/mkenvimage -s $(BR2_TARGET_UBOOT_ENVIMAGE_SIZE) \
 		$(if $(BR2_TARGET_UBOOT_ENVIMAGE_REDUNDANT),-r) \
+		-p 00 \
 		$(if $(filter "BIG",$(BR2_ENDIAN)),-b) \
 		-o $(BINARIES_DIR)/uboot-env.bin \
 		$(@D)/buildroot-env.txt
diff --git a/package/musl/musl.mk b/package/musl/musl.mk
index 5db5bbd265..2de1a425d0 100644
--- a/package/musl/musl.mk
+++ b/package/musl/musl.mk
@@ -57,8 +57,8 @@ endef
 
 define MUSL_INSTALL_TARGET_CMDS
 	$(TARGET_MAKE_ENV) $(MAKE) -C $(@D) \
-		DESTDIR=$(TARGET_DIR) install-libs
-	$(RM) $(addprefix $(TARGET_DIR)/lib/,crt1.o crtn.o crti.o rcrt1.o Scrt1.o)
+		DESTDIR=$(TARGET_DIR) install-libs install-tools install-headers
+#	$(RM) $(addprefix $(TARGET_DIR)/lib/,crt1.o crtn.o crti.o rcrt1.o Scrt1.o)
 endef
 
 $(eval $(generic-package))
--- a/package/c-periphery/c-periphery.mk	2025-09-02 05:41:22.385464415 +0000
+++ b/package/c-periphery/c-periphery.mk	2025-09-02 05:42:55.643061342 +0000
@@ -8,7 +8,7 @@
 C_PERIPHERY_SITE = $(call github,vsergeev,c-periphery,$(C_PERIPHERY_VERSION))
 C_PERIPHERY_INSTALL_STAGING = YES
 # only a static library
-C_PERIPHERY_INSTALL_TARGET = NO
+C_PERIPHERY_INSTALL_TARGET = YES
 C_PERIPHERY_LICENSE = MIT
 C_PERIPHERY_LICENSE_FILES = LICENSE

@@ -24,4 +24,10 @@
 	cp -dpfr $(@D)/src/*.h $(STAGING_DIR)/usr/include/c-periphery/
 endef

+define C_PERIPHERY_INSTALL_TARGET_CMDS
+        $(INSTALL) -D -m 0644 $(@D)/periphery.a $(TARGET_DIR)/usr/lib/libc-periphery.a
+        mkdir -p $(TARGET_DIR)/usr/include/c-periphery/
+        cp -dpfr $(@D)/src/*.h $(TARGET_DIR)/usr/include/c-periphery/
+endef
+
 $(eval $(generic-package))
